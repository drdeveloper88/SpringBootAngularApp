# SpringBootAngularApp
This project is a full-stack web application built using Angular (frontend) and Spring Boot (backend). It demonstrates a complete CRUD (Create, Read, Update, Delete) system where users can manage records through a REST API.  The application follows a client-server architecture.
